# Enforce LF line endings for all text files so Prettier
# produces identical output on Windows and Linux (CI).
* text=auto eol=lf

# Explicitly binary — no line-ending conversion
*.png  binary
*.jpg  binary
*.ico  binary
*.woff binary
*.woff2 binary
